export interface Feature {
    id: number;
    title: string;
    description: string;
    icon: string;
}

export interface Link {
    id: number;
    title: string;
    link: string;
}

export interface Statistique {
    id: number;
    title: string;
    description: string;
}